---
name: xxf-camera-wechat
description: :lib_camera_wechat 的外部接入说明，面向 Maven 消费方。
---

# :lib_camera_wechat

## What It Provides

`lib_camera_wechat` provides WeChat camera integration. Use the published Maven artifact from your app or feature module.

## Dependencies

- `lib_permission`

## Basic Usage

Use the public APIs exposed by this artifact. Keep the dependency on the published module only; do not rely on repository-internal build commands or local source paths.
