<?xml version="1.0" encoding="utf-8"?>
<resources>

    <attr name="shadow_startColor" format="reference|color"/>
    <attr name="shadow_topDelta" format="float"/>
    <!-- Selected Paint Color (color). -->
    <attr name="shadow_endColor" format="reference|color"/>
    <declare-styleable name="CardViewShadow">
        <!-- Default Paint Color (color). -->
        <attr name="shadow_startColor" />
        <!-- Selected Paint Color (color). -->
        <attr name="shadow_endColor" />
        <attr name="shadow_topDelta" />
    </declare-styleable>




    </resources>